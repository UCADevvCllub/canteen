import 'dart:ui';

const appColors = AppColors();
// Will be divided into light and dark themes

class AppColors {
  const AppColors();

  Color get primary => const Color(0xFF84C264);

  Color get darkGreen => const Color(0xFF228C22);
}